# FUNCTIONS — 01 Quick Actions & Composite Calls

## Quick Actions

- `remember()`
- `recall()`
- `learn()`
- `dream()`
- `analyze()`
- `validate()`

## Composite Calls

- `rememberAndLearn()`
- `analyzeAndSynthesize()`
- `cognize()`

## Usage Principle

Use quick actions for lightweight paths; use composite calls for full-cycle behavior.